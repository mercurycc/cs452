#include <types.h>
#include <err.h>
#include <config.h>
#include <context.h>
#include <task.h>

int task_setup( Context* ctx, Task* task, ptr stack, void (*code)() )
{
	uint* sp = (uint*)stack;

	stack 
}

int task_start( Context* ctx, Task* task )
{
	
}

